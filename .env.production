# Carregado automaticamente pelo Vite em `npm run build` (modo production).

# URL da API .NET (LayoutParserApi) em producao.
# Valor real usado hoje: IP hardcoded do servidor (mesmo IP que ja existe como
# fallback em src/services/api.ts e como proxy target em vite.config.ts).
# Isto e divida tecnica conhecida e documentada (ver "Pendencias conhecidas" em
# .claude/rules/frontend-standards.md e Roadmap do README): o ideal seria um
# dominio estavel (ex.: api.layoutparser.empresa.com), nao um IP interno.
# Ajuste aqui quando a infra de producao migrar para um dominio/DNS proprio.
VITE_API_BASE_URL=http://172.25.32.42:5000
