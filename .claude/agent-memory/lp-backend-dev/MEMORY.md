# Memory Index — lp-backend-dev (Dex)

- [Build travado pela API em execução](build-lock-api-em-execucao.md) — MSB3027 no `dotnet build/test` é lock da API dev, não erro de código; contornar com `-p:BaseOutputPath`.
